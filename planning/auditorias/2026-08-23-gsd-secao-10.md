# AUDITORIA — §10 do GSD: código construído fora do processo

> Procedimento de 12 passos do [`PADRAO-GSD.md`](../PADRAO-GSD.md) §10, aplicado ao código
> construído antes de o GSD virar padrão operacional.
>
> **Iniciada:** 23/08/2026. **Status: EM ANDAMENTO** — 4 dos 12 passos concluídos.
> Orientada a risco, como o §10 pede: não é reescrita indiscriminada.

---

## 🔴 Achado crítico — cadastro por e-mail e senha está quebrado em produção

`GAP DE REQUISITO CONFIRMADO` — atinge **REQ-001** (cadastro, login e sessão).

**Nenhum e-mail é enviado em produção.** As quatro funções de `lib/email.ts`
(`sendVerificationEmail`, `sendPasswordResetEmail`, `sendDeletionNotification`,
`sendCaregiverInviteEmail`) apenas escrevem um `logger.warn` quando `IS_PRODUCTION` é
verdadeiro. O envio real está comentado: `// TODO: integrar Resend ou SendGrid`.

A cadeia que isso produz, verificada linha a linha:

| Passo | Onde | O que acontece |
|---|---|---|
| 1 | `auth.ts:147` | cadastro cria a conta com `emailVerified: false` |
| 2 | `auth.ts:222` | auto-verificação só roda sob `allowsDevelopmentShortcuts()` — **falsa em produção** |
| 3 | `email.ts:39` | o e-mail de verificação **não é enviado**, só logado como aviso |
| 4 | `auth.ts:330` | login responde **403 "Verifique seu e-mail antes de fazer login"** |

**Resultado: quem se cadastra por e-mail e senha em produção nunca consegue entrar.**
Recuperação de senha tem o mesmo destino — o link nunca chega.

**Por que ninguém viu:** antes da correção de segurança de 21/08, a ausência de `NODE_ENV` fazia
`allowsDevelopmentShortcuts()` retornar verdadeiro, e as contas se auto-verificavam **em produção**.
A correção fechou corretamente um furo de segurança e, ao fazê-lo, **expôs este gap que já existia
por baixo**. Não foi detectado porque produção tem zero usuários e banco vazio.

**Saída de emergência que existe hoje:** o login com Google funciona
(`google-auth.ts:265`, `emailVerified: true`) e, melhor ainda, **conserta a conta presa** —
`google-auth.ts:233-235` marca `emailVerified: true` e `status: "active"` para um usuário existente
que ainda não estava verificado. Ou seja, quem travou pode se destravar entrando com Google
**usando o mesmo e-mail**. Isso não está documentado em lugar nenhum e não é óbvio para o usuário.

**Impacto por requisito:**

| Requisito | Situação |
|---|---|
| REQ-001 — cadastro e login | 🔴 quebrado por e-mail/senha; funciona só por Google |
| REQ-006 — exclusão de dados do titular | 🟠 a notificação aos cuidadores nunca chega (relevante para LGPD) |
| REQ-009 — convite de cuidador | 🟢 não afetado — o fluxo real usa link copiável e atalho de WhatsApp |

**Correção necessária antes de qualquer usuário real.** Três caminhos, em ordem de esforço:

1. **Integrar um provedor** (Resend/SendGrid). É o certo. Exige conta e chave — decisão do
   fundador, mesma natureza dos fornecedores já adiados.
2. **Mitigação imediata sem provedor:** deixar explícito na tela de cadastro que, sem e-mail
   configurado, a entrada é por Google — e o `logger.warn` virar alerta operacional visível
   em `/admin`, não uma linha perdida no log.
3. **Não fazer nada** só é aceitável enquanto produção seguir sem usuários. **Deixar assim no dia
   em que alguém real se cadastrar é entregar um app onde ninguém entra.**

---

## Restrições (CON-001 a CON-015) — primeira verificação da história do projeto

As 15 restrições são **violação bloqueante** segundo `config.json` (`phase_gates.blocking_violations`),
e nunca tinham sido conferidas contra o código. Resultado:

| CON | Restrição | Situação | Evidência |
|---|---|---|---|
| CON-001 | Não prescreve, sugere, calcula ou altera dose | 🟢 | Nenhum cálculo de dose. O "Calculando…" de `treatment-form.tsx:525` é a prévia de **horários** (motor de recorrência, REQ-012), não de quantidade |
| CON-002 | Sem checagem de interação medicamentosa | 🟢 | Zero ocorrências no código |
| CON-003 | Não dá orientação de saúde — encaminha | 🟢 | `RoutinePage` oferece **contato de emergência** cadastrado. Encaminhar, não avaliar |
| CON-004 | Não interpreta aferição | 🟢 **exemplar** | `health-measurements.ts` é estruturalmente incapaz: `value` é `z.string()`, nunca convertido para número nem comparado. O cabeçalho documenta a fronteira |
| CON-005 | Não é prontuário | 🟢 | Sem diagnóstico ou resultado de exame interpretado |
| CON-006 | Não automatiza recompra de controlado | 🟢 | Zero ocorrências |
| CON-007 | Logs sem medicamento, condição ou paciente | 🟢 | `safeLog` com allowlist + teste varrendo o código-fonte |
| CON-008 | Nenhum dado de saúde em URL, e-mail, SMS ou analytics | 🟢 | Os links carregam **só token** (`/verificar-email`, `/redefinir-senha`, `/convite`). Sem corpo de e-mail, não há o que vazar |
| CON-009 | Notificação não exibe medicamento por padrão | 🟢 | `show_medication_in_push` com `.default(false)`; ligar é escolha explícita da família |
| CON-010 | Nenhum vermelho em estado de dose | 🟢 | Todo vermelho é ação destrutiva (excluir paciente, sair do modo idoso, revogar dispositivo) ou o painel "o que NÃO fazemos" do `design-reference` |
| CON-011 | Nenhum texto responsabiliza o cuidador | 🟢 | Ocorrências de "culpa" são comentários **declarando a regra** |
| CON-012 | Sem gamificação | 🟢 | Ocorrências de "streak"/"ranking" são comentários dizendo que não existem |
| CON-013 | Segredos só em Secrets | 🟢 | Auditoria de 21/08 |
| CON-014 | Recurso de outra família devolve 404 | 🟢 | `family-access.ts`, com suíte dedicada |
| CON-015 | Nenhum `familyId`/`patientId` do cliente sem validação | 🟢 | Sempre do JWT |

**15 de 15 conformes.** É um resultado forte e vale registrar: as restrições foram respeitadas
mesmo sem ninguém as verificar formalmente, porque estavam no código como comentário e como teste,
não só no documento.

---

## Achados menores

| # | Achado | Onde |
|---|---|---|
| M-01 | `not-found.tsx` é o template shadcn **em inglês**, com texto de desenvolvedor vazando para o usuário: *"404 Page Not Found"* e *"Did you forget to add the page to the router?"*. Também usa `text-red-500` | `artifacts/zelo/src/pages/not-found.tsx` |

---

## Progresso dos 12 passos do §10

| # | Passo | Situação |
|---|---|---|
| 1 | Entender a arquitetura atual | 🟢 concluído (mapa em `decisoes/FOUNDATION.md`) |
| 2 | Mapear funcionalidades | 🟢 concluído (`HISTORIAS.md`) |
| 3 | Mapear requisitos | 🟢 concluído — 37 REQ + 5 REQ-F + 15 CON |
| 4 | Mapear fluxos de dados | 🔴 pendente |
| 5 | Identificar decisões existentes | 🟢 concluído (`decisoes/PLATFORM_DECISIONS.md`) |
| 6 | Identificar dívida técnica | 🟠 parcial — gaps em `STATE.md`, sem varredura sistemática |
| 7 | Identificar vulnerabilidades | 🟢 concluído (auditoria de 21/08, 9 correções) |
| 8 | Identificar ausência de testes | 🔴 pendente |
| 9 | Comparar estado atual com requisitos | 🟠 **em andamento** — CON 15/15 feitas; REQ-001 e REQ-006 auditados; faltam 35 REQ |
| 10 | Produzir plano de correção | 🔴 pendente |
| 11 | Executar correções por fases | 🔴 pendente |
| 12 | Verificar cada fase | 🔴 pendente |

**Próximo passo:** concluir o passo 9 varrendo os 35 requisitos restantes, e o passo 8 (cobertura
de teste), que é onde tende a aparecer requisito implementado mas nunca comprovado.
